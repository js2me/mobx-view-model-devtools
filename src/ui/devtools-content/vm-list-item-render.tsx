import type { ListItemViewProps } from '@/model/list-item/list-item';
import type { VMListItem } from '@/model/list-item/vm-list-item';

export const VMListItemRender = ({ item }: ListItemViewProps<VMListItem>) => {
  return 1;
};
